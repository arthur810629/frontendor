function nameAge(name, age) {
    if (age < 18) {
        return "Дорогой(ая) " + name + ", ты слишком молод(а)!";
    } else {
        return "Привет, " + name + "!";
    }
}

nameAge("Артур", 19)

let age = 10
let name = "Артур"